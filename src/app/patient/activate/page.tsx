"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function Activate(){const router=useRouter(),[code,setCode]=useState(""),[pin,setPin]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function activate(event:React.FormEvent){event.preventDefault();setLoading(true);setError("");try{const response=await fetch("/api/patient/activate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({code,pin,deviceLabel:"Web browser"})}),payload=await response.json();if(!response.ok)throw new Error(payload.message);router.push("/patient/check-in")}catch(reason){setError(reason instanceof Error?reason.message:"Activation could not be completed.")}finally{setLoading(false)}}return <main><p className="eyebrow">Patient · activation</p><h1>Welcome to the synthetic study</h1><form className="card grid" onSubmit={activate}><label>Activation code<input value={code} onChange={event=>setCode(event.target.value)} required/></label><label>Create a six-digit PIN<input inputMode="numeric" type="password" maxLength={6} value={pin} onChange={event=>setPin(event.target.value)} required/></label>{error&&<p role="alert" style={{color:"#9b2415"}}>{error}</p>}<button className="btn" disabled={loading}>{loading?"Activating…":"Activate this device"}</button></form></main>}
