import{describe,it,expect}from"vitest";import{claimAlert,closeAlert,consumeActivation,evaluatePlaceholderRule,hashPin,submitImmutable,upsertActiveAlert,verifyPin,type Answers}from"./domain";import{prototypeRouteAllowed}from"./access";
const base:Answers={pain:10,nausea:0,fatigue:0,eating:"normal",hydration:"none",sleep:"normal",functioning:"usual"};
it("gates staff and patient prototype boundaries",()=>{expect(prototypeRouteAllowed("production","true")).toBe(false);expect(prototypeRouteAllowed("development","true")).toBe(true)});
describe("high-risk milestone contracts",()=>{it("prevents activation reuse",()=>expect(()=>consumeActivation("USED",new Date(Date.now()+1000))).toThrow());it("validates and hashes a six-digit PIN",async()=>{const h=await hashPin("123456");expect(h).not.toBe("123456");expect(await verifyPin("123456",h)).toBe(true);expect(await verifyPin("12345",h)).toBe(false)});it("prevents response overwrite",()=>expect(()=>submitImmutable(base,base)).toThrow());it("creates deterministic urgent placeholder signal",()=>expect(evaluatePlaceholderRule(base)).toEqual({signals:["SYNTHETIC_TEST_PAIN_10","SYNTHETIC_TEST_NO_HYDRATION"],priority:"Urgent"}));it("aggregates without duplicate active alerts",()=>{const a=upsertActiveAlert(undefined,base)!;expect(upsertActiveAlert(a,base)?.id).toBe(a.id);expect(upsertActiveAlert(a,base)?.signals).toHaveLength(2)});it("enforces claim and close transitions",()=>{const a=upsertActiveAlert(undefined,base)!;expect(()=>closeAlert(a,"done")).toThrow();expect(closeAlert(claimAlert(a),"done").status).toBe("Closed")})});
